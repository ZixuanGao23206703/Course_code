#include <stdio.h>
#include <math.h>

int main() {
    double result = log(2.0); // calculate log(2)
    
    printf("log(2) = %lf\n", result); // output result£¬use %lf as the format specifier for double data type

    return 0;
}
